import 'package:flutter/material.dart';

class MenuModel {
  final int id;
  final String title;
  final IconData? icon;

  const MenuModel({
    required this.id,
    required this.title,
    required this.icon,
  });
}
