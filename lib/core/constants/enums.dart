enum HomeSectionType {
  posts,
  studentPortal,
  universityWebsite,
  studyPlan,
  lectureTables,
  examsTables,
  terms,
  references,
  qamoos,
}

enum DrawerItem {
  userProfile,
  aboutApp,
  kaloAnApp,
  links,
  appChannel,
  sendNote,
  settings,
  shareApp,
  rateApp,
  exit,
}

enum ImageType {
  asset,
  network,
  memory,
  file,
}
